Now I have the full picture. The crash chain is:Root cause: allowances.forEach { a -> key(a.processName) { AllowanceBarRow(...) } } in DashboardScreen.kt:371-375Why: The daily_allowances table uses process_name TEXT PRIMARY KEY, so it SHOULD be unique. But wait...Actually wait, I was wrong about my hypothesis. Let me re-read:The DashboardScreen at line 371 iterates allowances (not blockRules)allowances comes from Database.getDailyAllowances()Since process_name is the PRIMARY KEY in the schema, it's always enforced as unique — you can't have duplicate process names in that table even in legacy databases.Looking at how the allowances are used in the UI code, they're keyed by process name, so duplicates shouldn't be possible there either. But the error mentions exe names like "discord.exe" which could theoretically appear in both the block rules and allowances tables, though the primary key constraint would prevent duplicates within the allowances table itself.The real issue might be with legacy databases — if an older version of the app created the tables without the unique constraints on process names, then even though the current schema specifies process_name TEXT NOT NULL UNIQUE, those constraints wouldn't be enforced on existing data. So a user upgrading from an old version could have duplicate process names in their block rules table, which would cause the crash when the UI tries to render them with the same key. a duplicate key somewhere that was supposedly fixed before. Let me look at what the changelog says about those fixes to understand what keys were problematic. I'm realizing the crashes might be persisting because there's another code path still using an unsafe key. Looking back at the migration history, the issue mentions the same process being stored under different display-name casings through different enforcement paths — so even though we fixed the overlay and the items list, there could be a third location where this collision is still happening. The changelog reveals that the scanner itself can produce duplicate process names, and while the pickers were fixed to use composite keys (processName + index), I need to verify whether all lazy lists displaying scanned apps have been updated with the same fix. Let me check if any list is still using bare processName keys instead of the composite approach. a UUID, so duplicate IDs shouldn't happen unless the old code somehow set id = processName, which I've confirmed doesn't exist anywhere in the codebase. The crash must be coming from something else entirely, or there's a data corruption scenario I'm missing.Actually, I just realized the source column added in the v2 migration might be relevant—if that column fails to add for some reason, reading it could cause issues downstream. But that would likely crash the database query itself, not just the UI rendering.Let me stop overthinking this and apply the practical fixes: I need to change the items() function to use a more stable key instead of relying on id. The daily_allowances table has process_name as a PRIMARY KEY, so duplicates shouldn't be possible there. But I'm wondering if this table existed in the original schema or was added later — if legacy users had an older version without it, the migration would create it fresh with the constraint enforced, but users who already had the table might have a different schema.Regardless, I need to apply the fixes: modify AppBlockerScreen.kt to use indexed keys instead of just the ID, add defensive deduplication in getBlockRules(), and handle the daily allowances query similarly.For DashboardScreen.kt, I'm using forEachIndexed with a composite key that combines the process name and index position to ensure uniqueness in the Compose layout