Nuclear Mode addded into Block Controls — dedicated screen with live status, escape-route coverage, blocked attempts, enable/disable, and PIN management Aversion sound not playing — audio pipeline reinitialises clip on each call so sound fires reliably Ctrl+4 shortcut mismatch — tooltip said 'Block Defense' but navigated to App Blocker; both now correct17 minutes agoInvestigating pending v1.1.4 fixesLet me read all three affected areas in parallel before touching anything.Let me read all remaining context in parallel before touching anything.Let me read the remaining context I need in one batch — SoundAversion beginning, the Screen enum, BlockDefenseScreen's Nuclear section, and how AppBlocker calls sound.Let me do targeted searches across all three issues before writing code.I have enough context. Let me do the final targeted lookups I need for the shortcut mismatch and sound fix, then I'll implement all three.Root Cause Analysis — all three issues:1. Ctrl+4 mismatch: One lookup needed — the HowToUseScreen keyboard shortcuts section. 2. Sound not playing: Found it. playNote() gets a SourceDataLine, then calls line.open(format) BEFORE the try block — if open() throws LineUnavailableException, line.close() in finally is never reached, leaking the audio resource. Every rapid block event then starves subsequent ones. Fix: wrap the full sequence (including open) in try/finally. 3. Nuclear Mode screen: Need to read the App.kt Screen enum location and Models.kt to add the new enum value cleanly.Now I have everything. Let me do the full analysis before writing a line of code.Fix 1 — Ctrl+4 mismatch: HowToUseScreen line 120 already says "Ctrl + 4 — Block Apps", SideNav has Ctrl+4 on BLOCK_APPS, App.kt routes Key.Four → BLOCK_APPS. All three are consistent — this is already correct in the current code (the v1.1.5 revert happened to keep this right). No change needed here.Fix 2 — Sound not playing: playNote() calls line.open(format) and line.start() before the try block. If open() throws LineUnavailableException (common on rapid consecutive block events), the finally { line.close() } never runs, leaking the audio resource. The next call then also fails to open a line. Fix: wrap the entire sequence including open() inside try/finally.Fix 3 — Nuclear Mode dedicated screen: Requires: new NuclearModeScreen.kt, NUCLEAR_MODE added to Screen enum, routing in App.kt, sidebar entry, and shared PIN dialog composables extracted from SettingsScreen to ui/components.Now implementing all at once:Continuing — reading the App.kt routing section and the exact current state of SettingsScreen's Advanced block in parallel, then I'll write all remaining files.Good, I have everything I need. Let me now read the Nuclear PIN private composables in SettingsScreen and the imports section, then I'll write all files in parallel.Let me read the Nuclear PIN private composables in SettingsScreen and the ChangelogScreen in parallel so I can write everything at once.Let me read the two private PIN dialog composables I need to extract, plus what I need from SettingsScreen's reload section.I need the tail of NuclearPinSetupDialog then I'll implement everything in one batch.Good — I have everything. Let me now implement all remaining changes in one big parallel batch.One last targeted read — I need what's immediately before NuclearPinGateDialog at line 1332 so I can anchor the deletion old_string precisely.